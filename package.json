{
  "name": "contable-sheet-next",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "postinstall": "prisma generate",
    "vercel-build": "prisma generate && prisma migrate deploy && next build"
  },
  "dependencies": {
    "@prisma/client": "^5.22.0",
    "googleapis": "^169.0.0",
    "luxon": "^3.6.1",
    "next": "15.3.2"
  },
  "devDependencies": {
    "@types/luxon": "^3.6.2",
    "@types/node": "^20",
    "@types/react": "^19",
    "prisma": "^5.22.0",
    "typescript": "^5"
  }
}
